0.0e-06		# gravity: specific gravity acting in z-direction [N/mm3] = [1.0e+6 kN/m3]
10000 			# sig_yield: yield stress (= 2 * cohesion) [MPa] for volume elements (von Mises and Drucker-Prager material)
1.  			# shr_yield: yield stress [MPa] for interface elements (Cohesive material)
1.0   			# kn: interface elastic normal stiffness factor (0.0 < kn <1.0)
1.0   			# ks: interface elastic shear stiffness factor (0.0 < ks <1.0)
-100000  		# out_disp: +n: output total discplacment at step n ; -n: output incremental discplacement at step n (if n>last step then n=last step)
25  			# nstep: number of load steps per run (default = 10). nstep == 1: elastic analysis
20  			# iterat_max: max number of iterations per step - this triggers a scale-down and restart
1.0e-03  		# error_max: convergence tolerance (default = 1.0e-03)
1.2  			# relax: numerical over-relaxation (1.0 < relax < 1.5; default = 1.2)
2.0  			# scale_re: scale factor for re-start (default = 2.0)
1.2  			# scale_up: scale up for fast convergence (default = 1.2)
1.2  			# scale_dn: scale down for slow convergence (default = 1.2)
0.0			# friction angle (in degrees)
0.0			# dilation angle (in degrees)
1000.			# tensile strength [MPa]
1			# material model (1 = von Mises / Drucker Prager, 2 = Mohr Coulomb)
